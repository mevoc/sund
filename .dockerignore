# Keep the build context to the Go sources the image needs.
.git
.github
docs
tests
*.md
Makefile

# Deployment files (not needed to build the binary)
compose.yaml
compose.override.*
.env
.env.example

# Local build artifacts / data
/sund
/sund-*
dist
*.db
*.db-wal
*.db-shm
backup
.claude
